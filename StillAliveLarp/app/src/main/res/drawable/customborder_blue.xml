<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
    <solid android:color="@color/light_gray" />

    <!-- Rounded corners -->
    <corners android:radius="20dp" />

    <!-- Padding (only applies if using this as a <View>, not as a background) -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp" />

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="@color/blue" />

</shape>
